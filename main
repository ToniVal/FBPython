from pid import PIDController
from boolOp import boolOperators
from arithmeticOp import arithmeticOperators

""" # Example usage: # PID tester
desired_setpoint = 50
current_measurement = 30
kp = 0.6
ki = 0.005
kd = 0.1

pid = PIDController(desired_setpoint, kp, ki, kd)

for _ in range(100):
    control_output = pid.update(current_measurement)
    current_measurement += control_output  # Simulate a change in the system state
    print(f"Control Output: {control_output}")
 """

""" BOR = boolOperators.bNot(False) # Boolean operators

print(BOR) """

""" arithmetic_instance = arithmeticOperators() # Arithmetic operators

varOutT = "none"
nInp = arithmetic_instance.numInOut("input", 6, varOutT)

valu1 = nInp[0]
valu2 = nInp[1]
print(valu1)
print(valu2)

nOutp = arithmetic_instance.numInOut("output", valu1, valu2) #VITTU TÄÄ PASKA TOIMI SAATANA
print(nOutp) """

""" rooting = arithmeticOperators.numRoot(27,3)
print(rooting) """

FBSR = boolOperators.bSR(True, False)
print(FBSR)